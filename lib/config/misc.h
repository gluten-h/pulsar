#pragma once

#include "def_scene.h"


namespace PULSAR
{
	const size_t MAX_BINDABLE_COUNT = 512u;
	const size_t MAX_COMPONENT_COUNT = 512u;
	const size_t MAX_RG_COMPONENT_COUNT = 256u;
	
	const size_t MAX_SCENE_COUNT = 64u;
	const size_t MAX_CAMERA_COUNT = 8u;
}

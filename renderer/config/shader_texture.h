#pragma once

#include "config.h"
#include "shader_texture/shader_texture.h"


namespace pulsar
{
	const pulsar::shader_texture WHITE_QUAD_TEXTURE(pulsar::WHITE_QUAD_PATH);
	const pulsar::shader_texture BLACK_QUAD_TEXTURE(pulsar::BLACK_QUAD_PATH);
	const pulsar::shader_texture NORMAL_QUAD_TEXTURE(pulsar::NORMAL_QUAD_PATH);
}

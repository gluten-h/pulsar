#pragma once

#include <Windows.h>

#include <string>
#include <vector>
#include <sstream>
#include <memory>


namespace GRNG_UTILS
{
	std::vector<std::string>		str_split(const char *str, char delim);
}
